import { TCourseCreate } from '../interfaces'

const createHTMLCourse: TCourseCreate = {
    name: 'Frontend',
    description: 'HTML, CSS e JavaScript',
}

const createNodeCourse: TCourseCreate = {
    name: 'Backend',
    description: 'TypeScript com Express.js',
}

export { createHTMLCourse, createNodeCourse }
